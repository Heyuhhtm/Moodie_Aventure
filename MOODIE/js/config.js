/**
 * DilJourney Configuration
 * Update this file for production deployment
 */

window.APP_CONFIG = {
  // Change this to your production backend URL after deployment
  // Example: 'https://your-backend.onrender.com'
  apiUrl: ''  // Leave empty for localhost (development)
};
